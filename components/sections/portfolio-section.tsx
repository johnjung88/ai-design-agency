// Replaced by FeaturedMasonry on the home page and the /portfolio pages.
// Kept as a re-export shim so any existing imports don't break at the module level.
export function PortfolioSection() {
  return null;
}
